services:
  - type: web
    name: climate-intelligence-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -k gthread -t 120 -b 0.0.0.0:$PORT weather_app.api_server:create_app()
    healthCheckPath: /health
    autoDeploy: true
